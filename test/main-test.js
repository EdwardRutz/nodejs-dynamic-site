'use strict'

var expect = require('chai').expect

// Do a Sanity Check and Test mocha is installed and running correctly
describe('Mocha Installation', function() {
  it('should run mocha test using npm', function () {
    expect(true).to.be.ok
  })
})
